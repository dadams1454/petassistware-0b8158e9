
import ColorDistributionChart from './colorDistribution/ColorDistributionChart';

// Re-export the component to maintain the same import paths for all files using this component
export default ColorDistributionChart;
